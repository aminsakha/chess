package piece;

import Model.Block;
import javafx.scene.image.ImageView;

public class Knight extends Piece {
    public Knight(Block currentBlock, ImageView imageView) {
        super(currentBlock, imageView);
    }
}
