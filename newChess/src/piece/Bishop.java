package piece;

import Model.Block;
import javafx.scene.image.ImageView;

public class Bishop extends Piece {
    public Bishop(Block currentBlock, ImageView imageView) {
        super(currentBlock, imageView);
    }
}
