package piece;

import Model.Block;
import javafx.scene.image.ImageView;

public class Queen extends Piece {

    public Queen(Block currentBlock, ImageView imageView) {
        super(currentBlock, imageView);
    }
}
