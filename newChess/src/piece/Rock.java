package piece;

import Model.Block;
import javafx.scene.image.ImageView;

public class Rock extends Piece {

    public Rock(Block currentBlock, ImageView imageView) {
        super(currentBlock, imageView);
    }
}
