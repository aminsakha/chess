package piece;

import Model.Block;
import javafx.scene.image.ImageView;

public class King extends Piece {
    public King(Block currentBlock, ImageView imageView) {
        super(currentBlock, imageView);
    }
}
