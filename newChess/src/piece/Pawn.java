package piece;

import Model.Block;
import javafx.scene.image.ImageView;

public class Pawn extends Piece {

    public Pawn(Block currentBlock, ImageView imageView) {
        super(currentBlock, imageView);
    }
}
